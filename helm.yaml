appName: dispatch
service:
  type: ClusterIP
  port: 8080
ingress:
  enabled: false
configMap:
  enabled: true
  data:
    AMQP_HOST: rabbitmq-prod.roboshop.internal
    AMQP_USER: roboshop
externalSecret:
  enabled: true
